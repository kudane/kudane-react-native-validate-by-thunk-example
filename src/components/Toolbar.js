import React from "react";
import { ToolbarAndroid } from "react-native";

const Toolbar = () => (
  <ToolbarAndroid
    style={{ height: 56 }}
    title="Demo Validate Thunk"
    titleColor="darkgrey"
  />
);

export default Toolbar;
